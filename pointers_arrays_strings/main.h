#ifndef MAIN_H
#define MAIN_H

/**
 * _putchar - does the same as putchar()
 * return: Always 0.
 */

int _putchar(int c);
void reset_to_98(int *n);
void swap_int(int *a, int *b);
int _strlen(char *s);
void _puts(char *str);

#endif
