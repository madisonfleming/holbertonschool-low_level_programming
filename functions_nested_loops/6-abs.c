#include "main.h"
/**
 * _abs - compute the absolute value of an integer
 * @n: the integer to check
 * Description: _abs is the absolute value of int
 * Return: absolute value of n
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
return (0);
}
