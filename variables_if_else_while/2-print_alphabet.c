#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - entry point
 * Decription: for loop and putchar
 * Return: Success (0)
 */

int main(void)
{
	int i;

	for (i = 97; i <= 122; i++)
{
	putchar(i);
}
	putchar('\n');

return (0);
}
