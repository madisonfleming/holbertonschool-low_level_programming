#include <stdio.h>

/**
 * main - entry point
 * Description: printing numberz
 * Return: Success (0)
 */

int main(void)
{
	int num;

	for (num = 57; num <= 48; num--)
	{
		putchar(num);
	}
		putchar('\n');
return (0);
}
