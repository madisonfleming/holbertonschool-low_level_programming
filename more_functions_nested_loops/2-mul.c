#include "main.h"
/**
 * mul - multiplying two integers
 * @a: number to multiplied
 * @b: number be multiplied
 * Return: Result of multiplying a and b
 */
int mul(int a, int b)
{
	return (a * b);
}
