#include "main.h"
/**
 * _isupper - check for uppercase character
 * Return: 1 if uppercase, Return 0 otherwise
 * @c: character to check
 */
int _isupper(int c)
{
	if (c >= 65 && c <= 90)
{
	return (1);
}
	else
{
	return (0);
}
}
